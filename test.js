// test.js

// Import the library
const convertToDate = require('./index');

// Input string
const inputString = '2024-03-18';

// Convert the string to a date
const result = convertToDate(inputString);

// Print the result
console.log(result);